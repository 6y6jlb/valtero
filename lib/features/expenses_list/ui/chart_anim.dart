import 'package:flutter/animation.dart';

/// Shared fl_chart tween when legend visibility or series totals change.
const kChartAnimDuration = Duration(milliseconds: 350);
const kChartAnimCurve = Curves.easeOutCubic;
